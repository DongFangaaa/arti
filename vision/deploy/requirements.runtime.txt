# =============================================================================
# 视觉逻辑算法应用赛 — 容器运行时精简依赖（仅 ONNX 推理）
# =============================================================================
# 适用：Linux 服务器 / 测试环境 / CI 验证（不连接海康相机）
#
# 不包含 torch / torchvision / ultralytics（开发/训练用），镜像体积从
# ~2.5 GB 降到 ~600 MB。
#
# 如果现场需要切换回 Ultralytics（best.pt），请改用根目录的 requirements.txt
# 并修改 Dockerfile 的 COPY 指令。
# =============================================================================

# ---- 数值 / 图像基础 ----
numpy>=1.26,<2.0
opencv-python-headless>=4.8   # 无显示器环境：用 headless 版，省 X11 依赖

# ---- 配置 ----
PyYAML>=6.0
pydantic>=2.6

# ---- ONNX 推理（容器唯一模型后端）----
onnxruntime>=1.17            # CPU；如需 GPU 改为 onnxruntime-gpu 并装 CUDA base

# ---- 通讯 ----
pymodbus>=3.6

# ---- 日志 ----
# (stdlib logging 足够；不加第三方)